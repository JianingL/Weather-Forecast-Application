<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="FacebookLoginButton">
        <item name="android:layout_width">110px</item>
        <item name="android:layout_height">110px</item>
        <item name="android:layout_margin">10px</item>
        <item name="android:src">@drawable/fb_icon</item>
        <item name="android:background">@drawable/fb_icon</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

</resources>
